# Image tag to use
# Options: latest (main branch), develop, v1.0.0, or specific commit SHA
IMAGE_TAG=latest

# Image tag to pull from GitHub Container Registry
# Options: latest, develop, v1.0.0, or specific commit SHA
IMAGE_TAG=latest

# Node environment
NODE_ENV=production
